from Card import *

class DashCard(Card):
    
    def __init__(self):
        super().__init__()
        isDash = False
        cdOnDash = 1.0
        #On va utiliser isGrounded mais qui sera plutôt sur le joueur

    def Dash(self):
         while(self.isDash == False):
            if ('''isGrounded = False''' )

